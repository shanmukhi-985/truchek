/**
 * TruChek — Loading Page
 * Full-screen loading state for route transitions
 */

import React from "react";
import { PageLoader } from "@/components/ui/Loader";

export const LoadingPage: React.FC = () => <PageLoader label="Loading..." />;
